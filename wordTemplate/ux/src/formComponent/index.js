import React from "react";
import FormRenderer from "./container/formRenderer";

const FormikForm = ({ formMetaData }) => {
  return <FormRenderer formMetaData={formMetaData} />;
};

export default FormikForm;
